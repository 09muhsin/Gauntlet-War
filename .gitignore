# Secrets — never commit
.env
.env.local
*.key

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/

# Streamlit
.streamlit/secrets.toml

# Third-party course material — not ours to redistribute, and ~15MB of bloat.
# The doctrine distilled from it lives in src/knowledge.py.
/Marketing/

# Runtime / output (the committed demo set lives in /samples/, which is NOT ignored)
*.log
/runs/
/audit/*.json
!/audit/.gitkeep
/deliverables/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
